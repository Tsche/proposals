name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          submodules: 'recursive'

      - name: Get dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y make sudo gawk
          sudo apt-get install -y curl python3 python3-pip texlive-xetex python3-venv python3-requests pandoc
          sudo apt-get install -y nodejs npm 
          sudo npm install --global cowel

      - name: Build site
        run: python3 scripts/ci.py

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'generated'

  deploy:
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
